defmodule OpportunityWatcherTest do
  use ExUnit.Case, async: true
  doctest OpportunityWatcher

  # setup do
  #  {:ok, bucket} = KV.Bucket.start_link([])
  #  %{bucket: bucket}
  # end
  #
  # test "symbol update emits positive opportunities" do
  #  assert 1 == 2
  # end
  # test "symbol update does not emit negative opportunities" do
  #  assert 1 == 2
  # end
end
