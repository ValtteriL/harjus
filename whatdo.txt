<https://docs.aws.amazon.com/AWSEC2/latest/UserGuide/ena-improve-network-latency-linux.html>
<https://github.com/amzn/amzn-drivers/blob/master/kernel/linux/ena/ENA_Linux_Best_Practices.rst>
sysctl -w net.core.busy_read=1
sysctl -w net.core.busy_poll=1
sysctl -w net.ipv4.tcp_low_latency=1
ethtool -C interface adaptive-rx off rx-usecs 0 tx-usecs 0

Use minimal AMI with ENAv3 vailable on the majority of the EC2 Nitro v4 instance type < ubuntu since 20.04 has it

For Amazon Linux AMI, edit the /boot/grub/grub.conf file and add this option to the kernel line, as shown below:
kernel /boot/vmlinuz-4.14.62-65.117.amzn1.x86_64 root=LABEL=/ console=tty1 console=ttyS0 selinux=0 nvme_core.io_timeout=4294967295 xen_nopvspin=1 clocksource=tsc intel_idle.max_cstate=1 processor.max_cstate=1

realtime kernel <-- use latest ubuntu, install the rt kernel on it and TUNE IT
^ make sure the ena driver is in it

use 4 core system to be onthe safe side

# kernel

apt install linux-image-realtime
reboot
uname -r

# tuning
<https://documentation.ubuntu.com/real-time/latest/how-to/measure-maximum-latency/>
sudo apt install rt-tests
sudo cyclictest --mlockall --smp --priority=80 --interval=200 --distance=0 --loops=100000
